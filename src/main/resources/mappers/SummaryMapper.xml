<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kuro.kakeibocalendar.mapper.SummaryMapper">

    <resultMap id="DailySummaryResultMap" type="com.kuro.kakeibocalendar.domain.DailySummary">
        <result property="date" column="date" />
        <result property="incomeTotal" column="income_total" />
        <result property="expenseTotal" column="expense_total" />
        <result property="balance" column="balance" />
    </resultMap>

    <select id="findMonthlySummary" resultMap="DailySummaryResultMap">
        SELECT
            transaction_date AS date,
            SUM(CASE WHEN type = 'INCOME' THEN amount ELSE 0 END) AS income_total,
            SUM(CASE WHEN type = 'EXPENSE' THEN amount ELSE 0 END) AS expense_total,
            SUM(CASE WHEN type = 'INCOME' THEN amount ELSE 0 END)
              - SUM(CASE WHEN type = 'EXPENSE' THEN amount ELSE 0 END) AS balance
        FROM transactions
        WHERE transaction_date BETWEEN #{startDate} AND #{endDate}
        GROUP BY transaction_date
        ORDER BY transaction_date
    </select>

</mapper>